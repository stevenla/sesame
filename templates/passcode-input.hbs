<?xml version="1.0" encoding="UTF-8"?>
<Response>
	<Gather action="/passcode?passcode={{ passcode }}">
	    <Say voice="{{ voice }}" language="{{ language }}">{{ greetingMessage }}</Say>
	</Gather>
</Response>